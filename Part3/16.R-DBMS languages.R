# R-DBMS 관계형 데이터베이스 -> 표 형식, Oracle 이 대표적
# DBMS 1. 관계형 relational DBMs (Oracle, MySQL, MS-SQL)
# 2. 계층적 Hierarchiral
# 3. 네트워크 Network
# 4. NoSQL not only SQL --> 비정형 데이터까지 처리할 수 있는 것 (MongoDB)

#R-DBMS 에서 사용하는 language 는 크게 두 가지가 있따
#1. Data Control Language (DCL) 제어 -> DB 전문가, 관리자용 
#2. Data Manipulation Language (DML) 조작 -> 거의 99%같기 때문에 한 번 배우면 응용이 쉽다
#3. Data Definiton Language (DDL) 정의 -> 7-80 % 같음



















